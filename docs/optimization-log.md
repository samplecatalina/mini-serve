# Optimization log

Each entry records what was expected before a change was measured, what was measured, and why the two differ. Performance numbers link to rows under `results/` or reports under `profiling/`; correctness findings link to tests.

## M0 — Environment, reference model, correctness anchor

M0 has no performance changes. The entries below are the findings that shaped the test infrastructure every later change is judged by.

| Hypothesis | Prediction | Measured | Difference | Evidence |
|---|---|---|---|---|
| A FlashInfer op in the environment check exercises the runtime JIT toolchain (nvcc + host compiler), the path that breaks on cluster nodes with an incompatible host compiler. | An `rmsnorm` smoke test compiles through nvcc and leaves a built `.so` in the FlashInfer cache. | The test passed, but the JIT log was empty and no `.so` was produced: in flashinfer 0.6.7 `rmsnorm` defaults to CuTe DSL kernels compiled in-process. | The check verified numerics but not the toolchain. Switched to single-request decode attention (Qwen3 GQA shape), which is built by ninja + nvcc; the check now requires a JIT-built `.so`. Its `build.ninja` contains only `-gencode=arch=compute_89,code=sm_89`, no `-march`. | `miniserve/tools/env_check.py` (`check_flashinfer_jit`) |
| FlashInfer's cache location is controlled by `FLASHINFER_CACHE_DIR`. | Setting it redirects JIT artifacts out of `$HOME`. | flashinfer 0.6.7 reads only `FLASHINFER_WORKSPACE_BASE` (`flashinfer/jit/env.py`); artifacts land under `$FLASHINFER_WORKSPACE_BASE/.cache/flashinfer/<version>/<arch>/`. | Variable renamed in the image; the environment check asserts every cache directory resolves outside `$HOME`. | `Dockerfile`, `check_cache_dirs` |
| A plain-PyTorch Qwen3 that mirrors the transformers op order reproduces HF exactly. | Greedy tokens identical to HF; prefill logits bitwise equal. | 6 prompts, 1128 generated tokens (one 512-token run) identical; prefill logits over 563 positions bitwise equal (max abs diff 0.0). | None. | `tests/test_consistency.py` |
| Incremental decoding (prefill + one token per step) gives the same logits as one forward pass over the full sequence. | Bitwise equal, since the math is identical. | Max abs logit difference 0.34 over 32 steps; argmax flipped at one position where the top two logits (14.938 vs 14.875) were one BF16 ulp apart. HF shows the same flip between its full forward and `generate`. | Different GEMM shapes accumulate in different orders. The test bounds the difference (< 1.0) and requires equal argmax only where the top-2 gap exceeds 0.25. Consequence for batching: changing the batch dimension can flip near-tied tokens, which motivates the tolerance policy in `docs/design.md`. | `test_incremental_decode_matches_full_forward` |
| Token equality is a sufficient correctness anchor. | Any injected bug in the forward path changes greedy output. | Offsetting RoPE positions by one diverged at token 12. Setting one layer's `q_norm` weight to ones left 64 greedy tokens unchanged while shifting logits by up to 1.875. | Token equality misses small errors. The reference path therefore asserts bitwise prefill logits, which catches the `q_norm` mutation. | `test_prefill_logits_match_hf` |

## M1 — Continuous batching, paged KV cache, sampling, API

M1 has no performance measurements: its changes are judged by the correctness anchor and by stress tests, not by throughput. The tolerance policy used from here on: a batched or paged path may diverge from the reference only at a position where the reference's top-2 logit gap is at most ε = 0.5, and, fed its own output, the reference must rank each chosen token within ε of its maximum ("teacher-forced margin").

| Hypothesis | Prediction | Measured | Difference | Evidence |
|---|---|---|---|---|
| Running requests together (token-flattened batch, per-request contiguous KV) leaves greedy output unchanged except at near-ties. | Divergence from single-request output is rare and happens only where the top two logits are nearly equal. | Single-request path: 6/6 prompts token-identical to the reference. Concurrent: 5/6 (all at once) and 6/6 (staggered) sequences diverge within 128 tokens; at every divergence the reference top-2 gap is 0.0 or 0.125; teacher-forced margin at most 0.1875. A 32-request probe diverged on 42/64 sequences, 27 of them at exact BF16 ties. | Divergence is common, not rare: 2.1% of reference positions are exact ties, so a 128-token sequence very likely meets one, and any 1-ulp perturbation flips a tie. The margin near the top never exceeded 0.25. First-divergence checking alone missed an injected batching bug that the teacher-forced margin caught, so both checks are part of the anchor. | `tests/test_engine.py` (`test_concurrent_matches_reference`) |
| A reference-counted block allocator has no external fragmentation. | After any interleaving of allocations and frees, all free blocks can be allocated at once; internal waste per request is below one block. | Unit and randomized tests pass; 10/10 injected allocator bugs detected; forward-path results unchanged. | None. | `tests/test_block_allocator.py` |
| FlashInfer paged attention over 16-token blocks passes the same anchor as the contiguous path. | Even single-request output is no longer bitwise (a different kernel), but divergences stay within ε = 0.5. | Batch-1 decode logits differ from the reference by up to 0.68; 5/6 single-request prompts diverge, all at ties. A 32-request probe: 50/64 divergences, largest top-2 gap at a divergence 0.125, teacher-forced margin at most 0.25. Non-contiguous and descending block layouts pass. | As predicted for the kernel. Unexpected: shifting every decode position by one passed both token-level checks, because RoPE only sees relative positions and a uniform shift looks like an empty slot between prompt and output. An exact assertion on the positions fed to the model at every step now covers it. | `test_single_request_paged`, `test_paged_fragmented_pool`, `test_positions_contract` |
| Admission against a KV block budget with recompute preemption neither deadlocks nor changes output. | 64 mixed requests in a pool of at most 2048 tokens finish with preemptions, no out-of-memory error, and anchor-conforming output. | All at once, 2048-token pool: 234 steps, 11 preemptions, 13/64 divergences, gap ≤ 0.125, margin ≤ 0.1875. Staggered arrivals, 1536-token pool: 275 steps, 32 preemptions, 15/64 divergences, gap ≤ 0.125, margin ≤ 0.25. Positions exact at every step, including recomputed prefills; all blocks returned. A CPU simulation (30 randomized runs, 4–35 preemptions each) matches running each request alone token for token. | Preemption with recomputation adds no new numerical behaviour. Unexpected: preemption count is not monotonic in pool size (768 / 1024 / 1536 / 2048 tokens → 36 / 0 / 32 / 10) because long requests (~580 tokens) pack into the pool differently at each size. | `test_concurrent_matches_reference`, `test_simulated_load_under_kv_pressure`, `miniserve/tools/stress.py` |
| Sampling can run entirely on the device with one readback per step, and greedy rows are unaffected by sampled neighbours. | No host synchronization in the sampler; greedy rows equal `argmax`; sampled rows follow `softmax(logits / T)` restricted to the nucleus. | The sampler runs under CUDA sync-debug mode "error"; greedy rows equal `argmax` including ties and mixed batches; 50,000-draw frequencies match the target distribution within 0.01 and tokens outside the nucleus never appear; a request samples the same tokens at any row of any batch and after preemption. | FlashInfer's sampling kernels draw random numbers by row position (a request's sample depends on its neighbours), so sampling uses Gumbel-max with noise hashed from (request seed, token position, vocabulary index). Unexpected cost: the `[64, vocab]` temporaries peak at 510 MB, more than the activations of an 8192-token prefill, and the auto-sized KV pool shrank from 2772 to 2631 blocks. | `tests/test_sampler.py` |
| An engine thread behind a command queue, with an HTTP front end, serves streaming requests without changing output and without leaking KV blocks on cancellation. | Greedy text over HTTP equals offline output; cancelled requests return all blocks whether queued, decoding or requeued after preemption. | Greedy text over HTTP equals the offline text of the same request on the same engine. 32 concurrent HTTP requests in a 1536-token pool, 15 disconnected at random points: 21 preemptions, aborts reached the engine in both waiting and decoding states, and all blocks were free once idle. Streamed increments concatenate to the full decode and never contain half a character. | A test that only checks "all blocks free once idle" cannot show that cancellation works: a request that is never aborted also finishes and frees its blocks within the wait. The end-to-end test therefore also asserts that aborts reach the engine; an injected "disconnect never aborts" bug was caught only by that assertion. | `tests/test_server.py` |

## M2 — Prefix cache and chunked prefill

Measured on an RTX 4060 Laptop GPU with the engine-level harness (`bench/offline.py`: open-loop arrivals straight into the engine, settings alternated in one process, median of 3 runs per setting). Each prediction below was committed before the measurement it predicts. The share-of-peak figure is left out until this repository measures its own peak bandwidth and GEMM throughput.

| Hypothesis | Prediction | Measured | Difference | Evidence |
|---|---|---|---|---|
| Requests that share a long prompt prefix skip most of their prefill with a block-granular radix cache, and fit in the KV pool together. 64 requests in 8 groups sharing 1024-token prefixes (+64 own tokens, 64 output), all arriving at once, 32k-token pool. | Hit rate 0.75; prefill tokens 17,408 vs 71,844 (-76%); mean decode batch 64 vs 21; output throughput +114% (range +60% to +150%); TTFT p50 -80%. | Hit rate 0.75; prefill tokens 17,408 vs 71,844; decode batch 64.0 vs 21.3; **1487 vs 409 tok/s (+263%)**; TTFT p50 1239 vs 4630 ms (-73%); ITL p99 28 vs 633 ms. | Throughput far above the predicted range. The model charged decode for reading every sequence's KV from DRAM; in fact the attention kernel reads a shared block from DRAM once and serves the other sequences from L2, so a decode step over 64 requests costs about what one over 21 does (~29 ms per step either way under the profiler). The unshared baseline was predicted within 3%. | `results/rtx4060-laptop/m2_1_radix.csv`; `profiling/rtx4060-laptop/m2_1_radix_shared_burst.nsys-rep` |
| Decode attention over shared KV blocks moves only the distinct bytes through DRAM. One layer, 64 sequences of 1152 tokens (288 MiB of logical KV). | DRAM traffic falls toward the distinct KV when blocks are shared. | All distinct: 302.4 MB read from DRAM, L2 hit rate 0.15%. 8 groups sharing 1024-token prefixes: 67.4 MB (distinct KV: 64 MiB = 67.1 MB), L2 hit rate 77.7%. All sharing one prefix: 38.0 MB (distinct 36 MiB), 87.4%. | As hypothesized after the throughput surprise above. Kernel timings of this microbenchmark are not used: its all-distinct case ran into a software power cap at a lower clock. | `profiling/rtx4060-laptop/m2_1_decode_sharing_*.ncu-rep`; `results/rtx4060-laptop/m2_1_decode_sharing.csv` |
| With Poisson arrivals (4 req/s) the same workload gains in TTFT, not throughput; without sharing, the prefix cache costs nothing measurable. | Shared: hit rate 0.81, TTFT p50 -64% (range -40% to -75%), throughput unchanged. Unshared: within 3%. | Shared: hit rate 0.82, TTFT p50 31.0 vs 74.4 ms (-58%), throughput +0.2%. Unshared: within 1% (TTFT p95 -3.7%, the run-to-run spread of that percentile). | As predicted. | `results/rtx4060-laptop/m2_1_radix.csv` |
| Batching prefill chunks with decodes (decode first, per-step token budget) cuts the inter-token latency tail that whole prefills cause. 64 requests at 4 req/s, 8 of them 3072-token prompts (16 output), the rest 256-token prompts (128 output). | ITL p99: 512-token chunks -27%; **2048-token chunks +112%** (a stall spread over more steps pushes more gaps above the 99th percentile); ITL max -68% / -89%; long-prompt TTFT +18% / +61%; throughput unchanged. | ITL p99 44.9 (512) / 122.5 (2048) vs 60.9 ms unchunked (-26% / +101%); ITL max 48 / 143 vs 466 ms; long-prompt TTFT p50 285 / 224 vs 217 ms (+31% / +3%); throughput unchanged. | The ITL predictions held, including the worse tail with large chunks. Long prompts were slowed less than predicted because a chunk costs less inside a mixed step than the model assumed (next row). | `results/rtx4060-laptop/m2_2_chunked.csv` |
| Chunking costs some throughput when many prompts arrive at once (64 × 1088 tokens). | ITL p99 -74% (2048) / -90% (512); throughput unchanged to -10%. | ITL p99 129 / 47 vs 615 ms (-79% / -92%); **throughput 484 / 535 vs 420 tok/s (+15% / +27%)**; ITL p50 28.5 / 45.7 vs 28.3 ms. | Throughput went the other way. Decode-only steps leave the GPU 52–65% busy (CPU-side work per step); a mixed step with a 512-token chunk is 89% busy and takes ~44 ms against ~29 ms for a decode-only step, so its ~480 prefill tokens cost ~0.03 ms each against ~0.07 ms in prefill-only steps. The cost of small chunks shows in the median ITL instead (+62%). This effect should shrink once decode steps stop being CPU-bound. | `results/rtx4060-laptop/m2_2_chunked.csv`; `profiling/rtx4060-laptop/m2_2_chunked_unique_burst.nsys-rep` |

Correctness for both changes: all anchor tests pass with the prefix cache and chunked prefill enabled; a 563-token prompt prefilled in 64- or 100-token chunks gives last-position logits within 0.23 of one whole prefill on the reference path; concurrent, chunked and preempted runs pass the tolerance policy with exact positions at every step. With a prefix cache, a request's greedy output can differ at BF16 near-ties depending on what is cached, so exact comparisons start from an empty cache.

## M3 — CUDA Graphs, overlap scheduling, scheduling policies

Same harness and conventions as M2 (median of 3 runs per setting, settings alternated in one process, each prediction committed before its measurement; share of peak left out).

| Hypothesis | Prediction | Measured | Difference | Evidence |
|---|---|---|---|---|
| Replaying each decode step from a CUDA Graph removes the per-kernel launch cost of an eager step (hundreds of kernels issued from Python), which dominates small batches. | 4 requests at once: ITL p50 -25%, throughput +30%. 4 req/s stream (decode batches of about 8): ITL p50 -27%. 64 requests at once: within 3%, on the assumption that eager decode there is already GPU-bound. | 4 requests: ITL p50 10.1 vs 19.6 ms (**-48%**), 362 vs 194 tok/s (**+87%**). 4 req/s: decode step 8.45 vs 19.77 ms, ITL p50 8.6 vs 19.8 ms (-56%), ITL p99 30 vs 124 ms. 64 requests: 538 vs 475 tok/s (**+13%**). | Right direction, half the size. The prediction took the GPU time of an eager step with all its kernels queued in advance as the floor a graph can reach; a graph replay beat that floor, so kernels issued one by one cost GPU time as well as CPU time on this platform. In the 64-request burst the timeline shows the same GPU work per decode step either way (about 19 ms) with the GPU 61% busy in an eager step and 96% in a graph step: eager decode was not GPU-bound. | `results/rtx4060-laptop/m3_1_cuda_graph.csv`; `profiling/rtx4060-laptop/m3_1_cuda_graph_unique_burst.nsys-rep` |
| Chunked prefill raised burst throughput (M2) by filling GPU time that eager decode steps left idle, so with graphs the gain should shrink. | Stays near the eager +27% (the prediction, from the eager-floor measurement above, doubted that idle time existed). | 512-token chunks vs none, with graphs: 556 vs 460 tok/s (+21%). | The gain shrank by about 6 points, so idle-filling was part of it; the rest comes from mixed steps themselves (one forward pass for decode rows and a chunk instead of two). Cross-commit comparison, supporting evidence only. | `results/rtx4060-laptop/m3_1_chunked_under_graph.csv` |
| Overlap scheduling (launch step t+1 before reading back step t) hides the CPU part of each step; with decode graphs little CPU time is left to hide. | With graphs: 64 requests +2 to +5%, 4 requests +3%, 4 req/s ITL p50 -2 to -4%. Without graphs: +4 to +6%, bounded because an eager decode step is CPU-bound and overlap cannot make a step shorter than its CPU time. GPU busy fraction toward 99%. | With graphs: 64 requests 543 vs 528 tok/s (+2.9%), 4 requests +1.6%, 4 req/s ITL p50 8.55 vs 8.73 ms (-2.1%), ITL p99 -6.9%. Without graphs: 203 vs 190 tok/s (+7.2%). Over the 64-request run the GPU was idle 155 vs 344 ms (97.9% vs 95.6% busy). **TTFT p50 +4% (4 requests) and +26% (4 req/s, 38.8 vs 30.8 ms).** | Within or near the predicted ranges: a small result, as expected once decode is replayed from graphs. Not predicted: time to first token gets worse, since a sampled token is read back one step later, after the next step is launched; that costs about one decode step, which is large next to the 31 ms TTFT of short prompts. | `results/rtx4060-laptop/m3_2_overlap.csv`, `m3_2_overlap_eager.csv`; `profiling/rtx4060-laptop/m3_2_overlap_unique_burst.nsys-rep` |
| Admission order matters only while requests queue for KV. 128 requests at once in 16 groups sharing 512-token prefixes (32 own tokens), 25% asking for 512 output tokens and the rest for 32. | Pools of 6k / 8k tokens: cache-aware throughput +16% / +17%, mean latency -15%; shortest-job-first mean latency -49% / -42%, short requests -80% / -74%, long-request p99 +1% / -5% (a single burst does not starve long requests). 16k tokens: all within 5%. Cache-aware might gain more than predicted, as the model ignores cheaper decode over shared blocks. | 6k / 8k: cache-aware **+25% / +27%** throughput (947 / 1136 vs 758 / 894 tok/s), mean latency -25% / -24%, hit rate 0.79 / 0.78 vs 0.44 / 0.52; shortest-job-first mean latency **-49% / -42%**, short requests -80% / -74%, long-request p99 +2% / -4%, throughput -4% / +4%. 16k: throughput and latency within 5%, except cache-aware's ITL p99, 91 vs 32 ms. | Shortest-job-first matched the prediction closely. Cache-aware gained about 10 points more, in the direction noted beforehand. The ITL tail of cache-aware in the large pool was not predicted; it is repeatable (91 ms in all three runs) and sits at the length of a mixed step, which suggests that its admission pattern puts more decode gaps behind long mixed steps (unverified). | `results/rtx4060-laptop/m3_3_policy.csv`; `profiling/rtx4060-laptop/m3_3_policy_pool8192.nsys-rep` |

Correctness: every anchor test passes with graphs, overlap and each policy (the default settings run graphs and overlap, so the whole suite exercises them; concurrent anchors also run with each switched off). A decode graph's logits equal an eager step's bit for bit when no padding changes the batch shape, and stay within the tolerance when it does; padding rows write only a spare block, checked byte for byte over the whole pool. A test that queues a long sleep kernel between steps checks that launching a step never waits for the device; it found two blocking copies on the launch path, both fixed. Measuring the policies found a bug in the graph path: with a prefix cache, a batch's page indices can outnumber the pool's blocks, which overflowed a buffer sized to the pool; it is fixed and covered by a test, and earlier measurements did not share blocks across requests in decode.

## M4 — Primary benchmark on the L40S, ablations, head-to-head

Measured on a single NVIDIA L40S (Ada, 48 GB) on a Bridges-2 GPU node, with
[genai-bench](https://github.com/sgl-project/genai-bench) as the load generator from a separate container
image, over the OpenAI-compatible HTTP endpoint. Workload `D(1024,256)`: 1024-token prompts sampled from a
corpus, 256 output tokens, `temperature=0`, `ignore_eos=true`. Each concurrency level is a separate run; the
first 30 s of every run is discarded, because the clock needs about that long to settle. Every run writes a
sidecar with a 15-item fairness checklist, the clock and power trace, and the counters the engine itself kept
(the batch it actually formed, mixed-step share, preemptions), because the number of concurrent requests is
not the batch the engine forms and a load generator cannot see the latter. Each prediction below was committed
before the measurement it predicts.

The denominator is this repository's own measurement of device-to-device copy bandwidth (`bench/roofline.py`,
largest buffer size, since a decode step streams 1.19 GB of weights and no L2 can hold that): **654.5 GB/s** on
the L40S and **205.3 GB/s** on the RTX 4060 Laptop GPU. A decode step over a batch `B` at context `C` moves
`1.192 GB + B·C·112 KiB`, which turns any measured step time into a share of that floor — a figure that does
not depend on the device, the batch or the context, and so can be read across both machines.

| Hypothesis | Prediction | Measured | Difference | Evidence |
|---|---|---|---|---|
| Vendor specifications give the bandwidth step between the two devices, so the same engine scales by that ratio. | 3.8× to 4.5× (spec 864 vs a measured 200.6 GB/s). | Measured copy bandwidth 654.5 vs 205.3 GB/s: **3.19×**. The L40S reaches 75.8% of its 864 GB/s specification, the 4060 80.2% of its 256 GB/s. | The step is a fifth smaller than the specification implies, which is why every figure below uses a measured denominator. The same denominators show the engine on the 4060 sitting at **95.4% and 95.6%** of its bandwidth floor with graphs and a batch of 16 or more (71.8% eager, 85.0% at batch 4): on that device the scheduler has about 5% left to win, which is the reason the primary benchmark moved to the larger card. | `results/l40s/roofline.csv`, `results/rtx4060-laptop/roofline.csv` |
| With a pool that holds 256 sequences, output throughput approaches the decode bandwidth floor. 256 concurrent requests, pool 353,936 tokens, graph buckets to 256. | 4,100–4,350 tok/s, 85–90% of the 4,830 tok/s floor. Below 3,800 would mean a larger batch makes things worse. | **2,855.9 tok/s** at a formed batch of **243.31**. Measured throughput is **72.8 / 70.3 / 72.9 / 72.6%** of `batch ÷ TPOT` at concurrency 1 / 4 / 32 / 256. Decode steps themselves run at **81.6%** of the floor. | Wrong, and wrong in the method rather than the number: a decode-only ceiling was used against end-to-end throughput. A steady-state server also prefills for the same requests, and about 27% of the wall clock goes to steps that emit no output token — a share set by the 4:1 input-to-output ratio of the workload, not by the engine. Reading end-to-end throughput against a decode floor requires dividing by that share, or comparing TPOT against the floor instead. | `results/l40s/m4_2_main.csv` |
| Time to first token at 256 concurrency is dominated by queueing, so a pool that fits every request removes most of it. | 1.5–4 s, down from tens of seconds on the smaller card; the compute lower bound is 4.75 ms. | **TTFT p50 267.2 ms**, p95 496.8 ms. | Too pessimistic by an order of magnitude. On the 4060 the same workload gives 63,722 ms, because 256 concurrent requests there form a batch of 27.59 — identical to what 32 concurrent requests form (27.58), at identical throughput. Concurrency above what the pool holds changes only the wait. | `results/l40s/m4_2_main.csv`, `results/rtx4060-laptop/m4_1_main.csv` |
| Replaying decode from CUDA Graphs is worth more on this node than on the development machine, whose CPU is faster per core while its GPU is 3.19× slower. | +20% to +35% throughput, against +13.4% measured on the 4060. | **+129.4%** at concurrency 32 (TPOT 11.53 vs 25.94 ms) and **+0.9%** at concurrency 256 (61.89 vs 62.03 ms). | Both ends wrong, and the mechanism is the correction: eager mode costs 14.41 ms more per step at batch 31, where the GPU floor is only 8.11 ms, so the host becomes the bottleneck; at batch 243 the GPU step is 50.5 ms and overlap hides the same host time, leaving 0.14 ms. **The value of a graph is not a percentage but a condition** — it pays when a GPU step is shorter than the host work that issues it. The same card and the same workload move it from +129.4% to +0.9% by changing concurrency alone. | `results/l40s/m4_2_cuda_graph.csv` |
| Overlap scheduling (launch step t+1 before reading back step t) gains more here than the +1.6% measured on the 4060 with graphs on, because this node pairs a fast GPU with weak single-thread CPU. | +8% to +25%. Below +5% falsifies the standing prediction that the gain tracks the ratio of GPU speed to single-core CPU speed. | **+14.7%** at concurrency 32 and **+26.7%** at concurrency 256 (TPOT 61.89 vs 75.75 ms). | Within the range, slightly above at the top end, and the first supporting evidence for that prediction: the conclusion drawn on the 4060 — "with graphs, overlap buys nothing" — reverses on this node. The host time overlap hides is **1.82 ms/step at batch 31 and 13.85 ms/step at batch 243**: it *grows with batch* (scheduling 243 requests, packing block tables, sampling), while the launch cost a graph removes does not. The two mechanisms cover opposite ends. | `results/l40s/m4_2_overlap.csv`, `results/rtx4060-laptop/m3_2_overlap.csv` |
| Chunked prefill is a tail-latency change, best read as decode ITL p99. | ITL p99 improves; throughput roughly unchanged. | TPOT p99 moves **+3.2%** at concurrency 32 and **−1.9%** at 256 — nothing. What does move: **TTFT p50 −74.9% / −88.1%** (2,245.6 → 266.9 ms), sustained batch **142.63 → 243.39**, throughput +10.1% / +12.4%. | The chosen metric was the wrong one at this scale. Without chunking, a prefill-only step blocks the decode stream long enough that the pool never fills, so the effect shows up as a batch that cannot be sustained and a first token that arrives late, not as an inter-token tail. The metric for this feature is TTFT p50 together with the sustained batch. | `results/l40s/m4_2_chunked.csv` |
| Admission order matters only while requests queue for KV, so with a pool that holds everything the three policies converge. Pools of 40k / 100k / 354k tokens. | Cache-aware over FCFS: +15% to +30% at 40k, +5% to +15% at 100k, within ±3% at the full pool. | On the primary workload, **−2.9%**: prompts sampled at random from a corpus share almost no prefix, so there is nothing for a cache-aware order to exploit — and the load generator has no shared-prefix scenario. Re-run on the engine-level harness with 16 groups sharing a 512-token prefix: 64 requests, pools 6k / 8k / 16k / 41k → **+151.6% / +95.3% / −34.5% / −12.6%**; 512 requests, pools 41k / 102k / 354k → **+43.9% / +30.9% / +30.7%**. | The convergence prediction is falsified in both directions. At 64 requests there is a **crossover between 8k and 16k tokens**: once the pool stops evicting, FCFS reaches the same hit rate by itself (0.800 vs 0.759) and prefix ordering is left with only its cost, shrinking the batch from 53.8 to 26.0. Below the crossover the mechanism is direct — cache-aware recomputes 38% of the prefill tokens FCFS does (15,248 vs 39,936) at hit rates 0.781 vs 0.427. At 512 requests the full pool does not converge either: hit rates are equal there (0.801 vs 0.821) and cache-aware prefills *more* tokens, yet its batch is larger (55.6 vs 43.2) and its decode step faster (10.00 vs 15.07 ms). The leading explanation — co-scheduled requests share physical blocks that the 96 MiB L2 absorbs — needs per-step KV token counts and a profiler counter unavailable on this node, so it stays a hypothesis. | `results/l40s/m4_2_policy.csv`, `m4_2_policy_small.csv`, `m4_2_policy_large.csv` |
| Shortest-job-first trades the median against the tail. | Not predicted numerically. | Pool 40k, 256 concurrency: TTFT p50 20,362 → **113 ms (−99.4%)**, p95 23,174 → **69,927 ms (+201.8%)**. Pool 100k: −98.9% / +216.5%. | Reported because half of it is bad: the policy is a median-latency change bought with the tail, and the size of both halves depends on the pool. | `results/l40s/m4_2_policy.csv` |
| Against sglang 0.5.10 on the same node, in the same job, over localhost, with the same load generator and alternating ABBA over three rounds. | 60% to 85% of its throughput. | **61.6%** at concurrency 1, **89.9%** at 32, **101.7%** at 256 (2,867.1 vs 2,818.8 tok/s). With both engines' graph buckets capped at 32 instead of 256: 101.4%. | The range holds only at low concurrency; the ratio climbs with the batch, and the reason resolves to one quantity. Subtracting the measured bandwidth floor from TPOT leaves **2.14 / 3.55 / 13.07 ms** per step for this engine against **0.45 / 1.09 / 7.06 ms** for sglang — 4.8× / 3.3× / 1.85× — while a 47.8 ms GPU step at batch 248 swallows the remaining difference. That residual is everything the bandwidth model does not account for (non-attention kernels, sampling, the readback synchronization, planning work on the device, and host time), not host time alone: a component-by-component timing of this engine's host path puts it at 1.93 ms per step at batch 248 on the development host, and switching overlap scheduling off on the L40S exposes 13.85 ms of host work per step there. **The other half must be reported with it**: at 256 concurrency this engine's TTFT p50 is 275.7 ms against 1,994.1 ms, 7.2× better, while its TPOT is slightly worse. Its engine step is slower; its scheduler emits first tokens sooner. (sglang does not report the batch it forms, so the same floor was used for both sides at equal concurrency and equal completed requests — a reasonable proxy, not a measurement.) | `results/l40s/m4_2_compare.csv`, `m4_2_compare_bs32.csv`, `results/l40s/sglang_probe_server_info.json` |

Fairness, read back from both servers at run time: KV pool 334,992 vs 331,267 tokens (1.1% apart), chunked
prefill 4096 both, max prefill tokens 16384 both, max running 256 both, scheduling FCFS both, graph bucket cap
256 both, attention backend FlashInfer both. Recorded and not alignable: paging granularity (16-token blocks
vs page size 1), FlashInfer 0.6.7.post3 vs post2, torch cu128 vs cu129, and the bucket list below the cap.
Clocks cannot be pinned on either device; the sidecar carries the trace instead.

A second baseline against mini-sglang itself, the engine this one is modelled on, has not been run on this
platform yet, so only the sglang 0.5.10 percentages above are available.

Correctness: the same greedy anchor prompts were generated on all three devices used so far (RTX 4060 Laptop,
L40S, H100) from the same image and the same code. On every device the engine differs from that device's own
reference path only at BF16 near-ties, under the tolerance policy in `docs/design.md`.

### The second baseline: the blueprint on the development GPU

This engine was written from [mini-sglang](https://github.com/sgl-project/mini-sglang) at the pinned commit,
so the comparison that says most about its design decisions is against that, not only against a production
server. The blueprint ships a Dockerfile on that commit, from the same CUDA base image as this project's, so
it runs here in its own environment. Both engines have an in-process offline entry point that takes token ids
and returns token ids, so the comparison is engine to engine: no HTTP, no load generator, and neither side
tokenizes anything, which means a tokenizer difference cannot appear as an engine difference. The requests are
generated once and replayed on both sides; both import the same preflight, GPU sampling and warm-up rule.
Settings are matched from one place in the harness and read back off each engine afterwards, and the arm that
did not end up running what was asked refuses to write results.

Measured on the RTX 4060 Laptop GPU, KV pool pinned to 32,768 tokens on both sides (matched on tokens: the two
page it differently, 16 tokens per block here against one per page there), 256 running requests, graph buckets
to 256, 8192 tokens per forward pass, same weights snapshot, same torch build, alternating four processes of
three runs each, 30 s of warm-up discarded on both sides.

| Hypothesis | Prediction | Measured | Difference | Evidence |
|---|---|---|---|---|
| Two engines of the same architecture, on a workload the pool cannot hold all at once. 256 requests, 128-token prompts, 96 output tokens, nothing shared. | 85–100% of the blueprint, since this engine reached 101.7% of sglang 0.5.10 at 256 concurrency on the L40S and the blueprint is the simpler of the two. | **65.6%** (3,071.5 against 4,680.5 tok/s; run-to-run spread 0.12% and 0.25%). | Wrong, and the reason is the pool. That 101.7% was measured with a 335k-token pool, where 256 requests fit and scheduling barely matters; here the pool holds 32k and they do not. Admitting against a block budget puts more requests in flight than the pool holds: **81 preemptions recompute 46,519 prefill tokens against 32,768 in the prompts, 42% more**. The blueprint reserves each request's whole run up front and never preempts, so its prefill equals its prompts exactly. **The same design decision changes sign with the pool size.** | `results/rtx4060-laptop/m4_4_blueprint.csv` |
| The same comparison where a quarter of the requests ask for eight times as many tokens as the rest, so a reservation has to cover an unequal run. | Not predicted separately; run to test the attribution above. | **75.9%** (1,710.7 against 2,255.4 tok/s), with preemptions down from 81 to 18 and recomputed prefill down from 42% to 5.7%. | Moves with the preemption count and in the same direction, which is what the attribution predicts. | `results/rtx4060-laptop/m4_4_blueprint_mixed.csv` |
| A third workload, sixteen groups sharing a prefix, measured the same way. | — | The blueprint's **first measured run of every process was 19% slower than its next two** (4,546 against 5,629 tok/s), reproduced line for line in two independent processes, and a longer warm-up does not remove it. | Not an engine difference: repeating one workload leaves the previous run's blocks in the prefix cache, and the blueprint's pool keeps them where this engine's evicts them (its hit rate is identical on all six runs). Without shared prefixes the same effect is 4.2%; with nothing shared at all, 0.25%. **Alternating runs of one workload rewards whichever engine retains more of it**, so that ratio measures the protocol. Discarding the first run would be choosing data after seeing it; the numbers are kept as the evidence for the effect. | `results/rtx4060-laptop/m4_4_blueprint_policy_blueprint.csv` |

What these numbers do not cover, and it matters for reading them: every workload here runs with `ignore_eos`
and a fixed token count, so **no request ever finishes early**. The stated reason for admitting against a block
budget rather than reserving each request's whole run is that a reservation is wasted whenever a request stops
early — and that gain cannot appear in any of these measurements. A workload with stop tokens would be needed
to show it, and would break comparability with every earlier figure in this file, so it is not in the harness
today.

### A check before merging

Since M4 a change has to clear a short benchmark on the development GPU before it is merged (`make gate`):
two arms, 256 requests with nothing shared and the same requests behind a shared prefix, against a recorded
baseline in `results/rtx4060-laptop/gate_baseline.json`. 88 seconds, and it fails on a gain past the threshold
as well, because a baseline that is only ever allowed to be met stops being a baseline.

The threshold is 5%, which is worth stating against the noise it has to clear: four processes of three runs
each on one commit stayed inside 0.81%, runs within one recording inside 0.20–0.43%, and the drift across
commits that do not touch the engine is 0.5–0.7%. So the line sits about seven times the largest of those.
For a check a person runs by hand, a false alarm costs more than a missed one — a few false alarms and the
check gets skipped.

One thing the caliber has to pin, and one it must not. The KV pool is pinned: left to the engine it is derived
from whatever GPU memory is free, and one run of the check took 42,096 tokens where the baseline had 32,768 —
a larger pool preempts less, which reads as a gain that no change to the code caused. The chunked-prefill
budget is deliberately not pinned: it is the engine's own default, and a change to it is exactly what the
check exists to report.

## M5 — C++ scheduling core, speculative decoding, overlap across platforms

Each prediction below was committed before the measurement it predicts. Engine-level figures come from the
in-process offline harness (`bench/offline.py`: token ids in and out, no HTTP), alternating the two arms of an
ablation inside one process (ABBA), three runs per arm unless stated, median reported.

### The C++ core: block accounting, block-table packing, prefix tree

The core (`minicore/`, pybind11, built with a fixed `-march=x86-64-v2` baseline so the same build runs on the
cluster's CPUs) is a second backend behind the same interfaces as the Python reference, selected with
`--block-backend`. Every test in the suite runs against both. The reference stays the default; the reason is
the last row of this table.

| Hypothesis | Prediction | Measured | Difference | Evidence |
|---|---|---|---|---|
| The per-step host work that stays serial with the GPU is dominated by Python-side block accounting, so moving the allocator and block tables to C++ shortens the host path. Batch 248, context 1141, development host. | Host path 1.928 → 1.78–1.83 ms; end-to-end ±1% (graphs and overlap already hide most host time on this machine). | Host path **1.923 → 2.016 ms, 4.8% slower** (both backends re-measured in one later session). End to end 3,524.5 vs 3,527.9 tok/s: no measurable difference. | The direction was wrong. The accounting itself got faster, but its consumer did not move: every step, the packing of block tables into the attention kernel's paged metadata materialized 248 C++ vectors of 84 block ids as Python lists. **Moving a data structure across a language boundary while leaving its consumer behind makes the whole slower.** The unit of work to move is what one boundary crossing can finish. | `results/rtx4060-laptop/m5_1_5_host_breakdown_before_{python,cpp}.json`, `m5_1_block_backend.csv` |
| Moving the packing itself (block tables → `indptr / indices / last_page_len` written straight into resident pinned buffers, one call per batch) removes the regression and the host-to-device copy of Python lists. | C++ host path 1.0–1.3 ms; Python path 1.5–2.0 ms; C++ / Python ≤ 0.75; end-to-end ±1%. | C++ **0.920 ms**, Python **1.541 ms**, ratio **0.597**; packing and upload together 1.126 → 0.051 ms. End to end 3,542.7 vs 3,539.8 tok/s. | The host path is 40% shorter and the end-to-end number does not move. Both were expected: on this machine, with graphs and overlap, the host work serial with the GPU is about 0.3 ms per step. The largest remaining item is the attention library's own planning call (0.435 ms, 47% of the C++ path), which this repository does not own. | `results/rtx4060-laptop/m5_1_5_host_breakdown_after_{python,cpp}.json`, `m5_1_5_block_backend.csv` |
| A prefix tree whose eviction walks every node to collect leaves costs time proportional to its size, and a translation to C++ does not change that. 20,964-block pool, three tree shapes. | Same algorithm in C++: 5–20× faster; an ordered index of evictable leaves: ≤10 µs per eviction regardless of size. | On the largest tree (19,172 nodes), evicting one block: Python **5,553 µs**, C++ full walk **804 µs** (6.9×), C++ with the index **1.0 µs** (a further 800×). | Both as predicted, and the split is the point: **language 6.9×, algorithm 800×**. The index holds the evictable leaves ordered by last access and is updated wherever a node's lock count, leaf status or access time changes; tests step it against the full walk and require the same eviction order. The walk is still built in, for this comparison. | `results/rtx4060-laptop/m5_2_radix_host_scale_after_{python,cpp,cpp_walk}.json` |
| Cache-aware admission queries the tree for every waiting request each step; with 1,024 waiting that is the hottest host path in the scheduler. | Batched queries in Python: 1.147 → 0.45–0.75 ms per step; in C++: 0.08–0.25 ms. | **0.453 ms** (batched, Python) and **0.198 ms** (C++), of which the tree is 0.082 ms. End to end on the development GPU: 4,112.9 vs 4,108.1 tok/s. | Call granularity was worth more than the language: batching the queries alone removed 60%. The rest of the C++ figure is assembling the queue and sorting it, on the Python side. The timelines show the GPU busy through every step, so the saved host time is off the critical path here. | `results/rtx4060-laptop/m5_2_radix_host_engine_after_{python,cpp}.json`, `m5_2_block_backend_cache.csv`, `profiling/rtx4060-laptop/m5_2_cache_order_{python,cpp}.nsys-rep` |
| The end-to-end value of the core can only appear on a node whose CPU is slower per core. L40S node (Xeon 6740E, efficiency cores), 0.6B, up to 256 running, 335k-token pool. | End to end +0–6%; host path at batch 248 Python 3–7 ms, C++ 1.8–4 ms, ratio ≤ 0.7. | End to end **4,019.2 vs 4,019.2 tok/s** (0%). Host path **2.894 vs 1.890 ms** (0.653). | The host path is a third shorter and still does not reach throughput: a 44.2 ms GPU step at this batch, with overlap on, hides the whole difference. That is why the reference remains the default backend. One configuration still shows a signal worth a proper test — cache-aware admission with 4,096 waiting requests, where the tree is 21.6% of the step's wall clock in Python against 10.4% in C++ — but those two runs were sequential, not alternated, so it is not a result. | `results/l40s/m5_7_block_backend.csv`, `m5_7_host_breakdown_{python,cpp}.json`, `m5_7_radix_host_engine_{python,cpp}.json` |

### Two-model speculative decoding

A small draft model proposes γ tokens; the target verifies them in one forward pass and keeps the longest
agreeing prefix. Greedy output must equal the target's own, under the same near-tie tolerance as every other
path; that anchor runs on both the single-request and the concurrent path.

| Hypothesis | Prediction | Measured | Difference | Evidence |
|---|---|---|---|---|
| Acceptance is a property of the draft. Control: the target drafting for itself should accept everything. Qwen3-1.7B, development GPU. | Self-draft acceptance ≥ 0.95. | **0.970** at γ=2, **0.944** at γ=5. | Acceptance also contains the kernels: proposal runs through the decode kernel, verification through the prefill kernel, and at exact BF16 ties they choose differently; a round stops at the first one, so a longer γ loses more. From here on every acceptance figure is reported beside the self-draft one. | `results/rtx4060-laptop/m5_3_spec.csv` |
| A first version without graphs, 1.7B target / 0.6B draft, γ=4, eight sequences kept running. | 0.7–0.9× of the non-speculative path. | **0.99×** against the same path without speculation, **0.84×** against the default engine (graphs and overlap on); time to first token **+56%**. Decode alone: **+9.4%**. | The four numbers only mean something together: speculation won 9.4% on decode and the path gave up 15% by leaving graphs and overlap behind; the draft prefilled one request at a time. Also: with eight requests arriving at once, 0.77× — because speculation finishes requests sooner, the queue drains and the batch it forms is smaller (5.15 vs 7.21). Concurrency is not the batch the engine forms. | `results/rtx4060-laptop/m5_3_spec_saturated.csv`, `m5_3_baseline.csv` |
| Capturing the verification step in CUDA Graphs (bucketed by batch) and batching the draft's prefill removes most of the eager cost. | A round 71.0 → 50–58 ms; graphs on / off ≥ 1.2× in one process. | Round **57.80 ms**; graphs on / off **1.205×** in one process; first-token regression **+56% → +15%**. | At the top of the range. The round is now GPU-bound: the host waits in the readback. With a cheaper round **the best γ moves from 4 to 2** — a shorter γ wastes less when acceptance decays with it (0.81 / 0.70 / 0.62). | `results/rtx4060-laptop/m5_4_spec_saturated.csv`, `m5_4_round_graph.csv`, `m5_4_spec.csv`, `profiling/rtx4060-laptop/m5_4_round_{before,after}.nsys-rep` |
| At 8B / 0.6B on the L40S the draft costs 7.5% of the target by parameters, so speculation pays at small batches and loses at large ones. Caps 1–64, random 512-token prompts. | Batch 1: 1.3–1.8×. Batch 8, γ=4: a round at 70–90% of its bandwidth floor. | Against the default engine: **1.97× at 1**, 1.37× at 8, 1.03× at 32, **0.95× at 64** (best γ each). The crossover is between 32 and 64. A round at batch 8 reaches **62%** of its floor. | The draft does not cost 7.5%: on 142 SMs a 0.6B step at batch 8 is latency-bound, **5.2 ms against a 1.83 ms floor**, and the draft takes 44% of a round. Its real cost is the ratio of step times under a latency floor, not of parameters. The crossover depends on how full the batch stays, not only on where verification runs out of compute. | `results/l40s/m5_5_spec.csv`, `m5_5_baseline.csv`, `profiling/l40s/m5_5_round.nsys-rep` |
| Random prompts are neutral for acceptance. | Random ≥ natural text (it was, on 1.7B). | At γ=4: natural text **0.561**, random **0.431**; self-draft 0.994. | Reversed on 8B: after a random prefix the large model does not degrade and the small one cannot guess it. **The throughput figures above use random prompts and therefore understate speculation.** | `results/l40s/m5_5_alpha.csv` |
| Against sglang 0.5.10 in the same job on the same token ids: its two-model path (same algorithm) and EAGLE-3 with the public `Tengyunw/qwen3_8b_eagle3` head (chain of 4), each against its own engine's plain path. | Same acceptance for the two two-model paths (±5%); EAGLE-3 faster than both from cap 8 up. | Speedup at caps 1 / 8 / 64 — this engine two-model **1.96 / 1.36 / 0.91×**; sglang two-model **2.07 / 1.49 / 0.97×**; sglang EAGLE-3 **1.50 / 1.08 / 0.89×**. Tokens per round (random): two-model 3.13–3.66, EAGLE-3 1.80–2.16; natural text 3.07 vs 2.10. sglang's plain path is 1.06–1.22× this engine's. | Acceptance matched; the EAGLE-3 prediction was wrong in direction. This head, in a chain shape, emits about 1.4 fewer tokens per round than the same-family 0.6B draft, and a one-layer draft does not buy that back. **Same algorithm, same acceptance, sglang is 13–29% faster** — the gap is per-round cost, mostly the draft's steps. Scope: one head, chain shape only; tree verification and other heads were not measured. sglang's two-model path failed at caps 2 and 4 (it concatenates the target's and the draft's hidden states when merging a running batch with a new one); those cells are empty. | `results/l40s/m5_6_{ours,ours_spec,sglang,sglang_accept}.csv` |

### Overlap across three platforms

The same harness, the same workload (random 512-token prompts, 256 output tokens, four requests per slot
arriving at once, graphs on) and a pinned pool, so all three machines form the same batch. The gain is
on / off − 1 in output throughput.

| | CPU of the node | cap 8 | cap 32 | cap 128 |
|---|---|---|---|---|
| RTX 4060 Laptop, 0.6B | Ryzen 9 7945HX | +0.9% | +1.5% | — |
| L40S, 0.6B | Xeon 6740E (efficiency cores) | +5.4% | +5.2% | +6.8% |
| H100, 0.6B | Xeon Platinum 8470 | +3.2% | +5.2% | +10.9% |
| L40S, 8B | Xeon 6740E | +1.0% | +1.7% | +2.3% |
| H100, 8B | Xeon Platinum 8470 | +1.3% | +2.5% | +4.4% |

| Hypothesis | Prediction | Measured | Difference | Evidence |
|---|---|---|---|---|
| Overlap gains more as the GPU gets faster relative to one CPU core, and less as the model gets larger. | 0.6B: L40S above the 4060 at every cap, H100 at or above the L40S at caps 32 and 128. At every cap 8B below 0.6B. | Both hold (cap 32 a tie: 5.18% vs 5.23%, overlapping run-to-run ranges). **At cap 8 the H100 is below the L40S**, 3.2% against 5.4%. | The gain is close to *host time hidden per step ÷ step time*, and both terms had to be measured. Hidden host time (off − on decode step): cap 8 **0.24 → 0.11 ms** from L40S to H100, because the H100 node has faster cores; the step itself only **5.34 → 4.56 ms**, because a 0.6B step at batch 8 is held up by 3–4 ms that is not bandwidth. At cap 128 the step shrinks 2.3× and the host time only 1.3×, and the order flips. The variable in the hypothesis was right; the error was treating "GPU speed" as the bandwidth tier and the CPU as a constant. Across the three hosts the hidden host time at cap 32 is 0.18 / 0.25 / 0.40 ms (4060 host / H100 node / L40S node), in the order of their per-core speed. | `results/{rtx4060-laptop,l40s,h100}/m5_7_overlap.csv` |
| The bandwidth step between two cards predicts their throughput ratio to first order. 8B, cap 8, H100 against L40S. | 0.8–1.0 of the read-bandwidth ratio (2,926.8 against 746.8 GB/s, 3.92×). | Throughput ratio **2.53×, 0.65 of the bandwidth ratio**. | Each step carries about 5 ms that is not reading memory, on both cards: 27.52 ms = 22.97 ms floor + 4.55 on the L40S; 10.90 ms = 5.86 + 5.04 on the H100. A card with 3.9× the bandwidth shortens only the floor, so that fixed part grows from 17% to 46% of the step. Subtracting the floor first shows where the next gain has to come from. | `results/{l40s,h100}/m5_7_overlap.csv`, `results/{l40s,h100}/roofline.csv` (read, 1 GiB) |
| Where overlap's saved time comes from. | Not predicted. | With requests arriving at once, **24–42%** of the wall clock saved on the 0.6B runs on the cluster comes from mixed prefill-and-decode steps, which run eagerly; on 8B at small caps it is most of it (decode steps save 0.09–0.15 ms). | Overlap hides the launch cost of eager prefill steps as well as the host work of decode steps; the second half of that is invisible if only decode steps are examined. | same rows (`decode_step_ms`, `mixed_step_ms`) |
| A profiler timeline measures the host gap overlap removes. | Not predicted. | Under nsys the off arm's decode step on the L40S grows from 8.39 to 10.11 ms, the on arm's from 7.98 to 8.03 ms. Read from the timeline, the gain would be +24%; unprofiled it is +5.2%. | The profiler's per-launch cost lands on the host path, and overlap hides it. Magnitudes come from unprofiled runs; timelines are used for shape: GPU work per step equal in both arms, busy fraction 75–96% off and 93–99% on. | `profiling/{rtx4060-laptop,l40s,h100}/m5_7_*_overlap_{on,off}.nsys-rep` |
| The same engine's host path explains the 13.85 ms per step that switching overlap off exposed on the L40S server (M4). | The node's weaker cores stretch the host path by several times. | Host path at batch 248 on the L40S node **2.894 ms** against 1.541 ms on the development host, **1.88×**; the fastest-growing part is the attention library's planning call, 2.72×. | The engine's own step accounts for about 3 ms of the 13.85; the rest is outside it. The same card gives +5.2% from overlap offline and +14.7% over HTTP at the same concurrency, so the likely remainder is the server's own threads (HTTP, tokenization, streaming) sharing the process; the front end was not broken down, so that is an inference. The server-caliber overlap figure should not be quoted as a property of the scheduler alone. | `results/l40s/m5_7_host_breakdown_python.json`, `results/rtx4060-laptop/m5_1_5_host_breakdown_after_python.json`, `results/l40s/m4_2_overlap.csv` |

### Follow-ups to the blueprint comparison

Two gaps the M4 comparison against the blueprint left open, closed on the development GPU with the same
protocol (same token ids, same settings read back from both engines, ABBA).

| Hypothesis | Prediction | Measured | Difference | Evidence |
|---|---|---|---|---|
| The shared-prefix comparison could not be used because repeated runs reward the side that keeps last run's blocks. Emptying the blueprint's prefix cache before every timed run makes it usable. | The blueprint's first-run gap disappears. | It does: the blueprint's six runs fall within 4,414.0–4,427.8 tok/s (its first run had been 19% slower than the next two). This engine reaches **0.898×** the blueprint on that workload (3,968.4 vs 4,419.1 tok/s). | The published ratio for that workload is now 0.898; the earlier runs remain the evidence for why the protocol needed changing. | `results/rtx4060-laptop/m5x_blueprint_policy_cold_compare.csv` |
| Admitting against a block budget, with preemption, beats reserving each request's declared maximum when requests stop early — the gain none of the M4 workloads could show. New workload: short chat answers that stop at end of turn (about 46 tokens on average). | Not predicted numerically. | Declared maximum 512: this engine **1.40×** the blueprint (6,244.2 vs 4,451.6 tok/s). Declared maximum 128, same requests and same answers: **0.815×** (6,240.4 vs 7,655.9). | Changing only the declared maximum moves the blueprint 1.72× and this engine not at all, which isolates the admission policy as the cause. The two numbers are reported together: the blueprint's execution is faster; this engine wins when callers over-declare, as a default of 512 does for short answers. | `results/rtx4060-laptop/m5x_blueprint_chat_compare.csv`, `m5x_blueprint_chat128_compare.csv` |

### Not measured

Tree-shaped verification for EAGLE-3 and other draft heads; speculative throughput on natural-text prompts
(expected to be higher than the random-prompt figures above, by an amount not yet measured); a draft whose
several steps replay as one graph; an alternated test of the two prefix-tree backends under eviction pressure
on the L40S.
